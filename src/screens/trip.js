import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Trip = () => {
  return (
    <View style={{flex:1,justifyContent:"center",alignItems:"center"}}>
      <Text style={{color:"#000",fontSize:20}}>Trip</Text>
    </View>
  )
}

export default Trip

const styles = StyleSheet.create({})